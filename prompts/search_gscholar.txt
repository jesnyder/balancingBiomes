Write python code to acheive the following objective.
Accuracy is more important than speed.
Heavily comment the code to explain how and why the code works.

Include a header comment in the python code with the following:
* Date the code is generated

* Objective: List articles returned from a Google Scholar (https://scholar.google.com/) search for articles using the query ("Halophytes" AND "Halophile"). Artciles that are relevant to both halophile and halophyte. Search is not case sensitive.

* Tasks: List the major tasks to accomplish the goal in  sufficient detail for the work to be reproduced. The tasks are list as subheadings below with descriptions.

* Input: List the files, databases, and packages required to run the code

* Output: Lit files produced by the code

# Tasks to complete the objective

Write the code in a main function called
"search_gscholar"

## Search Google Scholar for Halophytes and Halophyle articles and Save Results for Each Page
https://scholar.google.com/
Use pagenation to collect articles 10 at a time
Wait 70 seconds between page queries to "HTTP 429 – Too Many Requests"
* If an error code is returned, stop submitting requests and continue with the code tasks
* If results are returned. Save the results of each page using a timestamp
For example, if the query is returned on January 31, 2025 at 12:30p for the first page of results (page 0), save the results as a list of dictionarys,
save to "results/search_results/gscholar/pages/202501311230_0000.json"
